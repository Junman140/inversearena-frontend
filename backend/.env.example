# Server
PORT=3001

# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/inversearena?schema=public

# MongoDB
MONGODB_URI=mongodb://localhost:27017/inversearena

# Admin auth
# Must be ≥32 characters (enforced at startup) to resist timing attacks
# ⚠️ Change this from the default value before deploying
ADMIN_API_KEY=change-me-in-production
# Admin confirmation token TTL in seconds. Minimum 300 (5 minutes).
# Set to 0 if you do not use confirmation tokens (API key auth only).
ADMIN_TOKEN_TTL_SECONDS=900
# Comma-separated Stellar public keys authorized to manage arenas from the
# /dashboard/admin frontend page (resolve/close/cancel rounds). Being
# "connected" is not enough — only addresses in this list pass GET
# /api/admin/wallet-role as isAdmin: true.
ADMIN_WALLET_ADDRESSES=

# Wallet auth (JWT)
JWT_SECRET=change-me-minimum-32-characters-long
# Rotation (#1456, docs/design/secret-rotation.md): during an overlap window the
# previous secret is verify-only until the expiry (ISO-8601, at most 30 days).
# JWT_SECRET_KID=
# JWT_SECRET_PREVIOUS=
# JWT_SECRET_PREVIOUS_KID=
# JWT_SECRET_PREVIOUS_EXPIRES_AT=2026-01-08T00:00:00Z
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d
NONCE_TTL_SECONDS=300

# Soroban / Stellar
SOROBAN_RPC_URL=https://soroban-testnet.stellar.org
STELLAR_NETWORK_PASSPHRASE=Test SDF Network ; September 2015
PAYOUT_CONTRACT_ID=C...
PAYOUT_SOURCE_ACCOUNT=G...
# Required: POST /api/arenas verifies the submitted txHash invoked create_pool
# on this contract before an arena row is written.
ARENA_FACTORY_CONTRACT_ID=C...
PAYOUT_METHOD_NAME=distribute_winnings

# Oracle webhook
ORACLE_WEBHOOK_SECRET=change-me-in-production
# Same rotation contract as JWT. Senders may name the key via `x-oracle-key-id`.
# ORACLE_WEBHOOK_SECRET_KID=
# ORACLE_WEBHOOK_SECRET_PREVIOUS=
# ORACLE_WEBHOOK_SECRET_PREVIOUS_KID=
# ORACLE_WEBHOOK_SECRET_PREVIOUS_EXPIRES_AT=

# Redis
REDIS_URL=redis://localhost:6379

# Rate limiting (defaults shown)
# RATE_LIMIT_NONCE_PREFIX=rl:auth:nonce
# RATE_LIMIT_NONCE_POINTS=5
# RATE_LIMIT_NONCE_WINDOW_SECONDS=60
# RATE_LIMIT_VERIFY_PREFIX=rl:auth:verify
# RATE_LIMIT_VERIFY_POINTS=10
# RATE_LIMIT_VERIFY_WINDOW_SECONDS=60
# RATE_LIMIT_REFRESH_PREFIX=rl:auth:refresh
# RATE_LIMIT_REFRESH_POINTS=20
# RATE_LIMIT_REFRESH_WINDOW_SECONDS=60
# RATE_LIMIT_POOLS_PREFIX=rl:pools:create
# RATE_LIMIT_POOLS_POINTS=3
# RATE_LIMIT_POOLS_WINDOW_SECONDS=60
# Maximum active SSE streams accepted by one backend process.
# SSE_MAX_CONNECTIONS_PER_IP=5
# SSE_MAX_CONNECTIONS_PER_ARENA=100

# Payout execution flags
PAYOUTS_LIVE_EXECUTION=false
PAYOUTS_SIGN_WITH_HOT_KEY=false

# Optional: only set in controlled environments
# PAYOUT_HOT_SIGNER_SECRET=S...

# Payout worker tuning
PAYOUTS_MAX_GAS_STROOPS=2000000
PAYOUTS_MAX_ATTEMPTS=5
PAYOUTS_CONFIRM_POLL_MS=2500
PAYOUTS_CONFIRM_MAX_POLLS=20
# Retry/dead-letter tuning for payouts stuck in "failed" status (#1122)
PAYOUTS_FAILED_RETRY_MAX=3
PAYOUTS_FAILED_RETRY_BASE_MS=5000

# resolveRound on-chain confirmation polling (#1193)
# Base delay between polls in ms (uses exponential backoff; mainnet can use a higher value)
ROUND_CONFIRM_POLL_MS=2500
# Maximum number of polls before giving up
ROUND_CONFIRM_MAX_POLLS=20

# Settlement receipts (#1407)
# Basis points (0-10000) of a round's yield portion withheld as a platform
# fee when computing the settlement breakdown (principal/yield/fee/dust).
# Defaults to 0 — no fee is withheld and payout amounts are unchanged from
# before this feature existed — unless explicitly set.
PLATFORM_FEE_BPS=0
